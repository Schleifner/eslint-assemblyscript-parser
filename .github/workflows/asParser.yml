name: asParser
on:
  pull_request:
    branches: test

jobs:
  asParser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: npm install
        run: npm install
      - name: run lint
        run: npm run lint
      - name: run mocha-test
        run: npm run test
